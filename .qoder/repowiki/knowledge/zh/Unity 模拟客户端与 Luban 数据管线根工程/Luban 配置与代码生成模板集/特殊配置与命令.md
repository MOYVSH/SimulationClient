在 `Template/` 目录下执行 `./gen.sh`（或 Windows 上的 `gen.bat`）即可生成全部目标；脚本硬编码 `LUBAN_DLL=$WORKSPACE/Tools/Luban/Luban.dll`，要求仓库根存在 `Tools/Luban/` 目录且已构建出 Luban 运行时。也可手动调用：`dotnet Tools/Luban/Luban.dll -t all -d json --conf Template/luban.conf -x outputDataDir=Template/output`。